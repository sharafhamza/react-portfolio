import React from "react";

const DescriptionTitle = ({ descriptionTitle }) => {
  return (
    <h4 className="text-primaryBlack text-21 font-medium">
      {descriptionTitle}
    </h4>
  );
};

export default DescriptionTitle;
